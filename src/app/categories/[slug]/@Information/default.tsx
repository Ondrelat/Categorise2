// app/categories/[slug]/@information/default.tsx
export default async function InformationDefault() {
  /*const synopsis = await getSynopsis();
  
  // Si pas de information, retourne null
  if (!information || information.length === 0) { */
    return null;
  /*}
  
  return (
    <div>
      <p>{information}</p>
    </div>
  );
  */
}